services:
  renewedtab:
    image: zebrajaeger/renewedtab:arm64-0.0.1-SNAPSHOT
    container_name: renewedtab
    restart: unless-stopped
    ports:
      - 8255:8255
    expose:
      - 8255
    volumes:
      - ~/data:/application/data
